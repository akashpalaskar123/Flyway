spring:
  application.name: Flyway-Demo
  datasource:
    url: jdbc:mysql://localhost:3306/flyway
    username: root
    password: Admin@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  flyway:
    baseline-on-migrate: true

server:
  port: 8082
  
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html